<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/connected_device_item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginStart="16dp"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="42dp"
        android:layout_marginBottom="12dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/textview_device_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:fontFamily="sans-serif-medium"
            android:layout_toStartOf="@id/button_disconnect"
            android:text="@string/device_name_label"
            android:textColor="@color/silabs_primary_text"
            android:textSize="@dimen/connection_item_device_name_text_size" />

        <Button
            style="@style/ButtonRed"
            android:id="@+id/button_disconnect"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:layout_gravity="end"
            android:layout_margin="4dp"
            android:text="@string/button_disconnect" />

        <ProgressBar
            android:id="@+id/progress_bar_connections"
            android:layout_width="82dp"
            android:layout_height="30dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:layout_gravity="right"
            android:text="@string/button_disconnect"
            android:textAllCaps="false"
            android:textColor="@color/silabs_white"
            android:textSize="@dimen/connection_item_progress_bar_text_size"
            android:visibility="gone" />
    </RelativeLayout>

</LinearLayout>