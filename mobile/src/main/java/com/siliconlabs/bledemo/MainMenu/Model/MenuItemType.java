package com.siliconlabs.bledemo.MainMenu.Model;

// If you add new menu item, you should add its identifier
// as enum. Define your own enum name,
public enum MenuItemType {
    BLUETOOTH_BROWSER,
    HEALTH_THERMOMETER,
    KEY_FOBS,
    ADVERTISER
}
